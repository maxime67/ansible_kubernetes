---
- name: Générer la commande de join
  command: kubeadm token create --print-join-command
  register: join_command_raw
  changed_when: false

- name: Sauvegarder la commande de join dans hostvars
  set_fact:
    kubeadm_join_command: "{{ join_command_raw.stdout }}"
